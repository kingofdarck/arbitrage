# 🚀 ПОЛНЫЙ ГАЙД ДЕПЛОЯ ОБЪЕДИНЕННОЙ СИСТЕМЫ

## 📋 Обзор системы

Объединенная система включает:
- 🤖 **Telegram бот** для управления и уведомлений
- 🔺 **Треугольный арбитраж** на Bybit с реальной торговлей
- 📊 **Умный монитор** арбитражных возможностей
- 🏥 **Health Check** сервис для мониторинга
- 💾 **Персистентные настройки** с сохранением в файл

## 🎯 Быстрый старт (Railway - рекомендуется)

### 1. Подготовка
```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd arbitrage

# Настройте переменные окружения
cp auto_arbitrage_bot/.env.example auto_arbitrage_bot/.env
# Отредактируйте .env файл с вашими ключами
```

### 2. Настройка .env файла
Отредактируйте `auto_arbitrage_bot/.env`:
```env
# ОБЯЗАТЕЛЬНО ЗАПОЛНИТЕ:
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
BYBIT_API_KEY=ваш_bybit_api_key
BYBIT_API_SECRET=ваш_bybit_secret

# Настройки торговли
TRADING_MODE=live
MIN_PROFIT_THRESHOLD=0.75
MAX_POSITION_SIZE=100.0
BYBIT_SANDBOX=false
```

### 3. Деплой на Railway
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в Railway
railway login

# Создайте проект
railway init

# Скопируйте конфигурацию
cp railway.unified.json railway.json
cp Dockerfile.unified Dockerfile

# Установите переменные окружения
railway variables set TELEGRAM_BOT_TOKEN=ваш_токен
railway variables set TELEGRAM_CHAT_ID=ваш_chat_id
railway variables set BYBIT_API_KEY=ваш_ключ
railway variables set BYBIT_API_SECRET=ваш_секрет

# Деплой
railway up
```

## 🐳 Docker деплой

### Простой Docker
```bash
# Сборка образа
docker build -f Dockerfile.unified -t unified-arbitrage .

# Запуск контейнера
docker run -d \
  --name unified-arbitrage \
  --restart unless-stopped \
  -p 8080:8080 \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/data:/app/data \
  --env-file auto_arbitrage_bot/.env \
  unified-arbitrage
```

### Docker Compose
```bash
# Скопируйте переменные окружения
cp auto_arbitrage_bot/.env .env

# Запуск
docker-compose -f docker-compose.unified.yml up -d

# Проверка логов
docker-compose -f docker-compose.unified.yml logs -f
```

## ☁️ Облачные платформы

### Render
1. Загрузите код в GitHub
2. Подключите репозиторий в Render Dashboard
3. Выберите `render.unified.yaml` как конфигурацию
4. Добавьте переменные окружения в Render
5. Запустите деплой

### Heroku
```bash
# Установите Heroku CLI
# Создайте приложение
heroku create your-app-name

# Скопируйте конфигурацию
cp Procfile.unified Procfile
cp app.unified.json app.json

# Установите переменные окружения
heroku config:set TELEGRAM_BOT_TOKEN=ваш_токен
heroku config:set BYBIT_API_KEY=ваш_ключ
# ... остальные переменные

# Деплой
git add .
git commit -m "Deploy unified system"
git push heroku main
```

## 🔧 Локальная установка

### Windows
```cmd
# Создайте виртуальное окружение
python -m venv venv
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
pip install -r auto_arbitrage_bot\requirements.txt
pip install aiohttp python-telegram-bot ccxt python-dotenv

# Запуск
python unified_system.py
```

### Linux/Mac
```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
pip install -r auto_arbitrage_bot/requirements.txt
pip install aiohttp python-telegram-bot ccxt python-dotenv

# Запуск
python unified_system.py
```

## 📊 Мониторинг и проверка

### Health Check
После деплоя проверьте:
- `http://your-domain/health` - статус системы
- `http://your-domain/` - основная страница

### Логи
```bash
# Docker
docker logs unified-arbitrage -f

# Docker Compose
docker-compose -f docker-compose.unified.yml logs -f

# Railway
railway logs

# Heroku
heroku logs --tail
```

### Telegram
Отправьте любое сообщение боту для проверки статуса системы.

## ⚙️ Настройка переменных окружения

### Обязательные переменные
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
BYBIT_API_KEY=ваш_bybit_api_key
BYBIT_API_SECRET=ваш_bybit_secret
```

### Настройки торговли
```env
TRADING_MODE=live                # test/live
MIN_PROFIT_THRESHOLD=0.75        # Минимальная прибыль %
MAX_POSITION_SIZE=100.0          # Максимальная позиция USD
BYBIT_SANDBOX=false              # true для тестовой среды
```

### Дополнительные настройки
```env
DEBUG=true                       # Режим отладки
LOG_LEVEL=INFO                   # Уровень логирования
WEB_ENABLED=true                 # Веб-интерфейс
WEB_HOST=0.0.0.0                # Хост веб-сервера
PORT=8080                        # Порт (для некоторых платформ)
```

## 🔐 Безопасность

### API ключи Bybit
1. Создайте API ключи в Bybit с правами:
   - Spot Trading
   - Read-Only (для баланса)
2. Ограничьте IP адреса если возможно
3. Используйте минимальные права доступа

### Telegram
1. Создайте бота через @BotFather
2. Получите токен бота
3. Найдите ваш chat_id через @userinfobot

## 🚨 Устранение проблем

### Проблема: Бот не отвечает
```bash
# Проверьте логи
docker logs unified-arbitrage | grep -i telegram

# Проверьте переменные окружения
echo $TELEGRAM_BOT_TOKEN
```

### Проблема: API ошибки Bybit
```bash
# Проверьте ключи
echo $BYBIT_API_KEY | wc -c  # Должно быть > 20
echo $BYBIT_API_SECRET | wc -c  # Должно быть > 30

# Проверьте права доступа в Bybit
```

### Проблема: Health check не работает
```bash
# Проверьте порт
curl http://localhost:8080/health

# Проверьте процесс
ps aux | grep python
```

## 📈 Оптимизация производительности

### Настройки для высокой нагрузки
```env
# Увеличьте лимиты
MAX_POSITION_SIZE=500.0
MIN_PROFIT_THRESHOLD=0.5

# Ускорьте проверки
CHECK_INTERVAL=3
BATCH_SIZE=100
```

### Мониторинг ресурсов
```bash
# Docker статистика
docker stats unified-arbitrage

# Использование памяти
docker exec unified-arbitrage ps aux
```

## 🔄 Обновление системы

### Обновление через Git
```bash
git pull origin main
docker-compose -f docker-compose.unified.yml down
docker-compose -f docker-compose.unified.yml up -d --build
```

### Обновление на Railway
```bash
railway up
```

## 📞 Поддержка

### Проверка статуса
1. Health check: `/health`
2. Telegram бот: отправьте любое сообщение
3. Логи: проверьте логи платформы

### Типичные проблемы
- **Короткие API ключи**: Убедитесь что ключи правильной длины
- **Блокировка IP**: Добавьте IP сервера в whitelist Bybit
- **Нехватка средств**: Пополните баланс на Bybit
- **Лимиты запросов**: Система автоматически соблюдает лимиты

## ✅ Чек-лист деплоя

- [ ] Настроен .env файл с реальными ключами
- [ ] Telegram бот создан и токен получен
- [ ] Bybit API ключи созданы с правильными правами
- [ ] Выбрана платформа деплоя
- [ ] Переменные окружения установлены
- [ ] Система задеплоена
- [ ] Health check работает
- [ ] Telegram бот отвечает
- [ ] Логи показывают нормальную работу
- [ ] Мониторинг настроен

## 🎉 Готово!

После успешного деплоя система будет:
- 🔄 Автоматически искать арбитражные возможности
- 📱 Отправлять уведомления в Telegram
- 💰 Исполнять прибыльные сделки (в live режиме)
- 📊 Предоставлять статистику и мониторинг
- 🛡️ Работать 24/7 с автоматическими перезапусками

Система полностью автономна и не требует постоянного вмешательства!