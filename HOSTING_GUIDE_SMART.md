# 🌐 Руководство по хостингу умного арбитражного монитора

## 🎯 Обзор

Это руководство поможет вам развернуть умный арбитражный монитор на VPS/облачном сервере для работы 24/7. Система будет автоматически отправлять уведомления о новых арбитражных возможностях в Telegram.

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────┐
│           VPS/Облачный сервер           │
├─────────────────────────────────────────┤
│  🧠 Smart Arbitrage Monitor             │
│  ├─ Поиск арбитражных возможностей      │
│  ├─ Дедупликация и отслеживание         │
│  └─ Отправка уведомлений в Telegram     │
├─────────────────────────────────────────┤
│  🏥 Health Monitor                      │
│  ├─ Мониторинг работоспособности        │
│  ├─ HTTP endpoint для проверок          │
│  └─ Уведомления о проблемах             │
├─────────────────────────────────────────┤
│  🐳 Docker + Docker Compose            │
│  ├─ Автоматический перезапуск           │
│  ├─ Изоляция процессов                  │
│  └─ Управление логами                   │
└─────────────────────────────────────────┘
```

## 🚀 Быстрый деплой

### 1. Подготовка сервера

**Минимальные требования:**
- 1 CPU core
- 1 GB RAM
- 10 GB SSD
- Ubuntu 20.04+ или CentOS 7+

**Рекомендуемые провайдеры:**
- DigitalOcean (от $5/месяц)
- Vultr (от $3.50/месяц)
- Linode (от $5/месяц)
- AWS EC2 t3.micro (бесплатный год)

### 2. Настройка Telegram бота

1. **Создайте бота:**
   - Напишите @BotFather в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен бота

2. **Получите Chat ID:**
   - Напишите сообщение вашему боту
   - Перейдите по ссылке: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   - Найдите `"chat":{"id":123456789}`
   - Сохраните Chat ID

### 3. Автоматический деплой

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd arbitrage-monitor

# Настройте Telegram в config.py
nano config.py

# Запустите автоматический деплой
chmod +x deploy.sh
./deploy.sh
```

## ⚙️ Ручная настройка

### 1. Установка Docker

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Перезайдите в систему для применения прав
exit
```

### 2. Настройка конфигурации

Отредактируйте `config.py`:

```python
# Настройки уведомлений
NOTIFICATION_CONFIG = {
    'telegram': {
        'enabled': True,
        'bot_token': 'ваш_токен_бота',  # Замените на ваш токен
        'chat_id': 'ваш_chat_id',       # Замените на ваш chat_id
    }
}
```

### 3. Запуск системы

```bash
# Создание директорий
mkdir -p logs

# Сборка и запуск
docker-compose up -d --build

# Проверка статуса
docker-compose ps
```

## 📊 Мониторинг и управление

### Просмотр логов

```bash
# Логи умного монитора
docker-compose logs -f smart-arbitrage-monitor

# Логи health monitor
docker-compose logs -f health-monitor

# Все логи
docker-compose logs -f
```

### Управление сервисами

```bash
# Перезапуск
docker-compose restart smart-arbitrage-monitor

# Остановка
docker-compose stop

# Полная остановка и удаление
docker-compose down

# Обновление кода
git pull
docker-compose up -d --build
```

### Health Check

Система предоставляет HTTP endpoint для мониторинга:

```bash
# Проверка здоровья
curl http://localhost:8000/health

# Ответ:
{
  "status": "healthy",
  "uptime_seconds": 3600,
  "uptime_human": "1:00:00",
  "total_cycles": 360,
  "total_opportunities": 45,
  "messages": ["Все системы работают нормально"]
}
```

## 🔧 Настройка автозапуска

### Systemd сервис (рекомендуется)

Создайте файл `/etc/systemd/system/arbitrage-monitor.service`:

```ini
[Unit]
Description=Smart Arbitrage Monitor
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/path/to/your/arbitrage-monitor
ExecStart=/usr/local/bin/docker-compose up -d
ExecStop=/usr/local/bin/docker-compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl enable arbitrage-monitor.service
sudo systemctl start arbitrage-monitor.service
```

### Cron для автоматических проверок

```bash
# Добавьте в crontab
crontab -e

# Проверка каждые 5 минут
*/5 * * * * cd /path/to/arbitrage-monitor && docker-compose ps | grep -q "Up" || docker-compose up -d
```

## 🛡️ Безопасность

### Настройка файрвола

```bash
# Ubuntu UFW
sudo ufw allow ssh
sudo ufw allow 8000/tcp  # Health check
sudo ufw enable

# CentOS firewalld
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload
```

### Обновления системы

```bash
# Автоматические обновления Ubuntu
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades

# Ручные обновления
sudo apt update && sudo apt upgrade -y
```

## 📈 Оптимизация производительности

### Настройка логов

В `docker-compose.yml` уже настроено ограничение логов:

```yaml
logging:
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"
```

### Мониторинг ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Использование диска
df -h

# Использование памяти
free -h
```

## 🚨 Устранение неполадок

### Частые проблемы

1. **Контейнер не запускается:**
   ```bash
   docker-compose logs smart-arbitrage-monitor
   ```

2. **Нет уведомлений в Telegram:**
   - Проверьте токен бота и chat_id в config.py
   - Убедитесь, что бот не заблокирован

3. **Высокое использование памяти:**
   ```bash
   docker-compose restart smart-arbitrage-monitor
   ```

4. **Проблемы с сетью:**
   ```bash
   # Проверка подключения к биржам
   curl -I https://api.binance.com/api/v3/ping
   ```

### Логи и диагностика

```bash
# Системные логи
journalctl -u docker.service -f

# Логи контейнера
docker logs smart-arbitrage-monitor

# Проверка health check
curl -v http://localhost:8000/health
```

## 📱 Настройка уведомлений

### Пример уведомления о запуске

После успешного деплоя система отправит:

```
🧠 УМНЫЙ АРБИТРАЖНЫЙ МОНИТОР ЗАПУЩЕН

⏰ Время запуска: 22:30:15 15.12.2024
🌐 Сервер: your-server-ip
📊 Настройки:
   • Минимальная прибыль: 0.75%
   • Проверка каждые: 10 секунд
   • Раздельные уведомления по типам

🎯 Система готова к поиску арбитражных возможностей!
```

### Уведомления о проблемах

При возникновении проблем:

```
🚨 ПРОБЛЕМА С УМНЫМ МОНИТОРОМ

⏰ Время: 14:32:15 15.12.2024
❌ Проблема: Лог файл не обновлялся 0:06:23

🔧 Рекомендации:
1. Проверьте логи: docker-compose logs smart-arbitrage-monitor
2. Перезапустите: docker-compose restart smart-arbitrage-monitor
3. Проверьте интернет соединение

🌐 Health check: http://localhost:8000
```

## 💰 Стоимость хостинга

### Примерные расходы в месяц:

| Провайдер | Конфигурация | Цена |
|-----------|--------------|------|
| DigitalOcean | 1 CPU, 1GB RAM | $5 |
| Vultr | 1 CPU, 1GB RAM | $3.50 |
| Linode | 1 CPU, 1GB RAM | $5 |
| AWS EC2 | t3.micro | $0 (первый год) |
| Hetzner | CX11 | €3.29 |

### Дополнительные расходы:
- Домен (опционально): $10-15/год
- SSL сертификат: бесплатно (Let's Encrypt)
- Мониторинг: бесплатно (встроенный)

## 🎯 Заключение

После настройки система будет:

✅ **Работать 24/7** без вашего участия  
✅ **Автоматически перезапускаться** при сбоях  
✅ **Отправлять уведомления** только о новых возможностях  
✅ **Мониторить свое здоровье** и сообщать о проблемах  
✅ **Логировать всю активность** для анализа  

**Система готова к продуктивной работе!** 🚀