# 🚀 ПОЛНЫЙ ГАЙД ПО ДЕПЛОЮ ТРЕУГОЛЬНОГО АРБИТРАЖА

## 📋 ВАРИАНТЫ ДЕПЛОЯ

### 🆓 **БЕСПЛАТНЫЕ ВАРИАНТЫ (рекомендуется для начала)**
1. **Railway** - 500 часов бесплатно в месяц
2. **Render** - 750 часов бесплатно в месяц  
3. **GitHub Codespaces** - 60 часов бесплатно в месяц
4. **Heroku** - ограниченный бесплатный план

### 💰 **ПЛАТНЫЕ ВАРИАНТЫ (для профессионального использования)**
1. **VPS (DigitalOcean, Linode)** - $5-20/месяц
2. **AWS EC2** - от $3/месяц
3. **Google Cloud** - от $5/месяц

---

## 🚀 ВАРИАНТ 1: RAILWAY (РЕКОМЕНДУЕТСЯ)

### Преимущества:
- ✅ 500 часов бесплатно в месяц
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка
- ✅ Автоматические перезапуски

### Шаги деплоя:

#### 1. Подготовка файлов
Создайте файлы в папке `auto_arbitrage_bot/`:

**railway.json:**
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python bybit_live_triangular.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

**requirements.txt:**
```txt
ccxt>=4.0.0
python-dotenv>=1.0.0
aiohttp>=3.8.0
asyncio-mqtt>=0.11.0
websockets>=11.0.0
```

**Procfile:**
```
worker: python bybit_live_triangular.py
```

#### 2. Загрузка на GitHub
```bash
# Инициализация Git (если еще не сделано)
git init
git add .
git commit -m "Треугольный арбитраж готов к деплою"

# Создайте репозиторий на GitHub и загрузите
git remote add origin https://github.com/ваш-username/arbitrage-bot.git
git push -u origin main
```

#### 3. Деплой на Railway
1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Python проект

#### 4. Настройка переменных окружения
В Railway панели:
1. Перейдите в "Variables"
2. Добавьте переменные:
```
BYBIT_API_KEY=ваш_api_key
BYBIT_API_SECRET=ваш_secret
BYBIT_SANDBOX=false
TRADING_MODE=live
MIN_PROFIT_THRESHOLD=0.5
MAX_POSITION_SIZE=50.0
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=ваш_telegram_token
TELEGRAM_CHAT_ID=ваш_chat_id
```

#### 5. Запуск
Railway автоматически запустит бота после деплоя!

---

## 🔧 ВАРИАНТ 2: VPS (ПРОФЕССИОНАЛЬНЫЙ)

### Рекомендуемые провайдеры:
- **DigitalOcean** - $5/месяц (1GB RAM, 25GB SSD)
- **Linode** - $5/месяц (1GB RAM, 25GB SSD)
- **Vultr** - $2.50/месяц (512MB RAM, 10GB SSD)

### Шаги настройки VPS:

#### 1. Создание VPS
1. Зарегистрируйтесь на DigitalOcean
2. Создайте Droplet:
   - **OS**: Ubuntu 22.04 LTS
   - **Plan**: Basic $5/месяц
   - **Region**: ближайший к вам

#### 2. Подключение к серверу
```bash
ssh root@ваш_ip_адрес
```

#### 3. Установка зависимостей
```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и pip
apt install python3 python3-pip git screen -y

# Установка зависимостей Python
pip3 install ccxt python-dotenv aiohttp asyncio-mqtt websockets
```

#### 4. Загрузка кода
```bash
# Клонирование репозитория
git clone https://github.com/ваш-username/arbitrage-bot.git
cd arbitrage-bot/auto_arbitrage_bot

# Настройка .env файла
nano .env
```

Добавьте в .env:
```env
BYBIT_API_KEY=ваш_api_key
BYBIT_API_SECRET=ваш_secret
BYBIT_SANDBOX=false
TRADING_MODE=live
MIN_PROFIT_THRESHOLD=0.5
MAX_POSITION_SIZE=50.0
```

#### 5. Запуск в фоне
```bash
# Создание screen сессии
screen -S arbitrage

# Запуск бота
python3 bybit_live_triangular.py

# Отключение от сессии (Ctrl+A, затем D)
# Для возврата: screen -r arbitrage
```

---

## 🐳 ВАРИАНТ 3: DOCKER (УНИВЕРСАЛЬНЫЙ)

### Создание Docker контейнера:

#### 1. Dockerfile
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Установка зависимостей
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копирование кода
COPY . .

# Создание директории для логов
RUN mkdir -p logs

# Запуск бота
CMD ["python", "bybit_live_triangular.py"]
```

#### 2. docker-compose.yml
```yaml
version: '3.8'

services:
  arbitrage-bot:
    build: .
    container_name: triangular-arbitrage
    restart: unless-stopped
    environment:
      - BYBIT_API_KEY=${BYBIT_API_KEY}
      - BYBIT_API_SECRET=${BYBIT_API_SECRET}
      - BYBIT_SANDBOX=false
      - TRADING_MODE=live
      - MIN_PROFIT_THRESHOLD=0.5
      - MAX_POSITION_SIZE=50.0
    volumes:
      - ./logs:/app/logs
    networks:
      - arbitrage-network

networks:
  arbitrage-network:
    driver: bridge
```

#### 3. Запуск Docker
```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

---

## 📱 МОНИТОРИНГ И УПРАВЛЕНИЕ

### 1. Создание Telegram бота для управления
```python
# telegram_control_bot.py
import os
import subprocess
from telegram import Update
from telegram.ext import Application, CommandHandler, ContextTypes

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("🤖 Бот управления арбитражем запущен!")

async def status(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # Проверка статуса основного бота
    try:
        result = subprocess.run(['pgrep', '-f', 'bybit_live_triangular.py'], 
                              capture_output=True, text=True)
        if result.stdout:
            await update.message.reply_text("✅ Арбитражный бот работает")
        else:
            await update.message.reply_text("❌ Арбитражный бот не запущен")
    except Exception as e:
        await update.message.reply_text(f"❌ Ошибка проверки: {e}")

async def restart_bot(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("🔄 Перезапуск арбитражного бота...")
    # Логика перезапуска

def main():
    app = Application.builder().token("ваш_telegram_bot_token").build()
    
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("status", status))
    app.add_handler(CommandHandler("restart", restart_bot))
    
    app.run_polling()

if __name__ == "__main__":
    main()
```

### 2. Настройка автозапуска (для VPS)
```bash
# Создание systemd сервиса
sudo nano /etc/systemd/system/arbitrage-bot.service
```

Содержимое файла:
```ini
[Unit]
Description=Triangular Arbitrage Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/arbitrage-bot/auto_arbitrage_bot
ExecStart=/usr/bin/python3 bybit_live_triangular.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация сервиса:
```bash
sudo systemctl daemon-reload
sudo systemctl enable arbitrage-bot
sudo systemctl start arbitrage-bot

# Проверка статуса
sudo systemctl status arbitrage-bot
```

---

## 📊 МОНИТОРИНГ ЛОГОВ

### 1. Просмотр логов в реальном времени
```bash
# Для VPS
tail -f logs/bybit_live_triangular.log

# Для Docker
docker-compose logs -f arbitrage-bot

# Для Railway
railway logs --follow
```

### 2. Настройка ротации логов
```bash
# Создание конфигурации logrotate
sudo nano /etc/logrotate.d/arbitrage-bot
```

Содержимое:
```
/root/arbitrage-bot/auto_arbitrage_bot/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
```

---

## 🛡️ БЕЗОПАСНОСТЬ И БЭКАПЫ

### 1. Настройка файрвола (для VPS)
```bash
# Установка ufw
sudo ufw enable
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp
```

### 2. Автоматические бэкапы
```bash
#!/bin/bash
# backup.sh
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/root/backups"
SOURCE_DIR="/root/arbitrage-bot"

mkdir -p $BACKUP_DIR
tar -czf $BACKUP_DIR/arbitrage_backup_$DATE.tar.gz $SOURCE_DIR

# Удаление старых бэкапов (старше 7 дней)
find $BACKUP_DIR -name "arbitrage_backup_*.tar.gz" -mtime +7 -delete
```

Добавление в crontab:
```bash
crontab -e
# Добавить строку:
0 2 * * * /root/backup.sh
```

---

## 🚨 УСТРАНЕНИЕ ПРОБЛЕМ

### Частые проблемы и решения:

#### 1. "API key is invalid"
```bash
# Проверка переменных окружения
echo $BYBIT_API_KEY
echo $BYBIT_API_SECRET

# Тестирование ключей
python3 test_new_api_keys.py
```

#### 2. "Out of memory"
```bash
# Увеличение swap (для VPS с малой RAM)
sudo fallocate -l 1G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

#### 3. Бот не запускается
```bash
# Проверка зависимостей
pip3 install -r requirements.txt

# Проверка синтаксиса
python3 -m py_compile bybit_live_triangular.py

# Запуск в debug режиме
python3 -u bybit_live_triangular.py
```

---

## 📈 ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ

### 1. Настройка для максимальной производительности
```env
# В .env файле
MIN_PROFIT_THRESHOLD=0.3    # Снижаем порог для большего количества сделок
MAX_POSITION_SIZE=100.0     # Увеличиваем размер позиции
TRADING_MODE=live           # Реальная торговля
```

### 2. Мониторинг ресурсов
```bash
# Установка htop для мониторинга
sudo apt install htop

# Мониторинг использования ресурсов
htop

# Мониторинг сетевой активности
sudo apt install iftop
sudo iftop
```

---

## 🎯 РЕКОМЕНДУЕМАЯ СХЕМА ДЕПЛОЯ

### Для начинающих:
1. **Railway** - простой деплой, бесплатно
2. Тестовый режим первые дни
3. Малые суммы для торговли

### Для опытных:
1. **VPS** - полный контроль
2. **Docker** - изоляция и масштабируемость  
3. **Мониторинг** - Telegram бот + логи
4. **Бэкапы** - автоматические

### Профессиональная схема:
1. **Несколько VPS** в разных регионах
2. **Load balancer** для распределения нагрузки
3. **Мониторинг** - Grafana + Prometheus
4. **Алерты** - PagerDuty или аналоги

---

## 🚀 БЫСТРЫЙ СТАРТ

### Самый простой способ (Railway):
```bash
1. Загрузите код на GitHub
2. Зайдите на railway.app
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения
5. Деплой готов!
```

### Команды для быстрого деплоя:
```bash
# Клонирование и настройка
git clone https://github.com/ваш-repo/arbitrage-bot.git
cd arbitrage-bot/auto_arbitrage_bot

# Настройка .env
cp .env.example .env
nano .env  # Добавьте ваши API ключи

# Тестирование
python3 test_new_api_keys.py

# Запуск
python3 bybit_live_triangular.py
```

**Система готова к деплою! Выберите подходящий вариант и запускайте! 🚀**